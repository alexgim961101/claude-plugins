---
description: Create a PRD (Product Requirements Document) for a new project
argument-hint: [project description]
allowed-tools: [Read, Glob, Grep, Bash, Write]
---

# Init Command

The user has invoked `/init` with the following input:

**Arguments:** $ARGUMENTS

## Instructions

1. Read `skills/init/SKILL.md` to load the full PRD creation process.
2. If `$ARGUMENTS` is empty, ask the user to describe the project before proceeding.
3. Otherwise, treat `$ARGUMENTS` as the initial project description and follow the process in SKILL.md exactly.
